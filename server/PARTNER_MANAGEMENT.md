# 🔐 Управление Учетными Записями Партнеров

## Быстрые Команды

Все команды выполняются из папки `server`:
```bash
cd server
```

### 1️⃣ Посмотреть Всех Партнеров
```bash
node list-partners.js
```

### 2️⃣ Создать Нового Партнера
```bash
# 1. Откройте файл и измените данные:
nano create-partner.js

# 2. Найдите секцию PARTNER_DATA и измените:
const PARTNER_DATA = {
    email: 'entrepreneur@example.com',  // ← Новый email
    password: 'secure123',              // ← Новый пароль
    name: 'Новый Предприниматель',      // ← Имя
    role: 'PARTNER'
};

# 3. Запустите:
node create-partner.js
```

### 3️⃣ Изменить Существующего Партнера
```bash
# 1. Откройте файл:
nano update-partner.js

# 2. Измените данные:
const OLD_EMAIL = 'partner@aura.com';           // ← Старый email
const NEW_EMAIL = 'newpartner@example.com';     // ← Новый email
const NEW_PASSWORD = 'newpassword123';          // ← Новый пароль

# 3. Запустите:
node update-partner.js
```

---

## 📝 Примеры Использования

### Пример 1: Создать партнера для демо
```bash
# В create-partner.js установите:
email: 'demo@business.com'
password: 'demo2026'
name: 'Demo Business'

# Запустите:
node create-partner.js
```

### Пример 2: Сменить пароль существующему партнеру
```bash
# В update-partner.js установите:
OLD_EMAIL = 'partner@aura.com'
NEW_EMAIL = 'partner@aura.com'  # Оставить тот же
NEW_PASSWORD = 'newSecurePass123'

# Запустите:
node update-partner.js
```

### Пример 3: Создать несколько партнеров для разных предпринимателей
```bash
# Запустите create-partner.js несколько раз с разными данными:

# Партнер 1
email: 'business1@example.com'
password: 'pass1'
name: 'Бизнес 1'

# Партнер 2  
email: 'business2@example.com'
password: 'pass2'
name: 'Бизнес 2'
```

---

## ⚠️ Важные Замечания

1. **Безопасность**: Не используйте простые пароли в production
2. **Email уникален**: Нельзя создать двух партнеров с одинаковым email
3. **Роли**: 
   - `PARTNER` - может управлять своими товарами
   - `ADMIN` - полный доступ к платформе
   - `USER` - обычный покупатель

---

## 🎯 Для Демо Предпринимателям

Создайте отдельного партнера для каждого предпринимателя:

```bash
# Предприниматель 1
node create-partner.js
# email: entrepreneur1@test.com
# password: demo123

# Предприниматель 2
node create-partner.js  
# email: entrepreneur2@test.com
# password: demo123
```

Затем дайте им учетные данные для входа!
